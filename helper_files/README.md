# Helper scripts

Utilities for local development only; not used by the main application.

- **list_directory.py** – Prints the project directory tree (skips `.venv`, `__pycache__`, `.git`). Run with `python helper_files/list_directory.py` from the repo root.
